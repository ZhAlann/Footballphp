<?php
//$url = 'https://api.football-data.org/v4/matches'; // Remplacez par l'URL de l'API que vous souhaitez appeler

//$ch = curl_init();

// Configurer les options de cURL
//curl_setopt($ch, CURLOPT_URL, $url);
//curl_setopt($ch, CURLOPT_RETURNTRANSFER, true); // Récupérer la réponse dans une variable au lieu de l'afficher
// Autres options de cURL...

// Exécuter la requête et récupérer la réponse
//$response = curl_exec($ch);

// Vérifier les erreurs
//if (curl_errno($ch)) {
//echo 'Erreur lors de la requête API : ' . curl_error($ch);
// Gérer l'erreur de manière appropriée
//}

// Fermer la connexion cURL
//curl_close($ch);

// Traiter la réponse de l'API
// ...
?>
<div class="page">
    <h2>Pas encore de contenu</h2>
</div>